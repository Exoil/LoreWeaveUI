/**
 * The Foundry module id (must match `module.json`). Every settings key, hook
 * name, CSS class, and DOM id is namespaced with this — import it, never inline
 * the literal twice (see `.claude/rules/foundry.md`).
 */
export const MODULE_ID = 'loreweaveui';
